# -*- mode: ruby -*-
#: set ft=ruby :

# All Vagrant configuration is done below. The "2" in Vagrant.configure
# configures the configuration version (we support older styles for
# backwards compatibility). Please don't change it unless you know what
# you're doing.

  # The most common configuration options are documented and commented below.
  # For a complete reference, please see the online documentation at
  # https://docs.vagrantup.com.
  # Every Vagrant development environment requires a box. You can search for
  # boxes at https://vagrantcloud.com/search.
require_relative 'config'
  
Vagrant.configure("2") do |config|
  
  # --- SERVER VM ---
  config.vm.define "server" do |server|
    server.vm.box = $config_settings[:box]
    server.vm.hostname = "nfs-server"
    server.vm.network "private_network", ip: $config_settings[:server_ip]
    
    server.vm.provider "virtualbox" do |vb|
      vb.name = "nfs-server-vm"
      vb.memory = $config_settings[:server_memory]
      vb.cpus = $config_settings[:server_cpus]
    end

    server.vm.provision "shell", path: "scripts/common.sh"
    server.vm.provision "shell", path: "scripts/server.sh"
  end

  # --- CLIENT VM ---
  config.vm.define "client" do |client|
    client.vm.box = $config_settings[:box]
    client.vm.hostname = "nfs-client"
    client.vm.network "private_network", ip: $config_settings[:client_ip]
    
    client.vm.provider "virtualbox" do |vb|
      vb.name = "nfs-client-vm"
      vb.memory = $config_settings[:client_memory]
      vb.cpus = $config_settings[:client_cpus]
    end

    client.vm.provision "shell", path: "scripts/common.sh"
    client.vm.provision "shell", path: "scripts/client.sh"
  end


  ##Testing
  # # -*- mode: ruby -*-
  # # vi: set ft=ruby :

  # # Global hosts entry definition for manual /etc/hosts resolution
  # hosts = <<-HOSTS
  # 192.168.56.10 web.local web
  # 192.168.56.11 db.local db
  # HOSTS

  # Vagrant.configure("2") do |config|
  #   # Global base box configuration
  #   config.vm.box = "cloud-image/ubuntu-24.04"

  #   # Hostmanager plugin configuration for automated host/guest name resolution
  #   if Vagrant.has_plugin?("vagrant-hostmanager")
  #     config.hostmanager.enabled = true
  #     config.hostmanager.manage_host = true
  #     config.hostmanager.manage_guest = true
  #     config.hostmanager.ignore_private_ip = false
  #     config.hostmanager.include_offline = true
  #   end

  #   # ==========================================
  #   # Database Machine (db)
  #   # ==========================================
  #   config.vm.define "db" do |db|
  #     # Per-machine box override example (bento/ubuntu-24.04 as referenced in guide patterns)
  #     # db.vm.box = "bento/ubuntu-24.04"

  #     db.vm.hostname = "db.local"

  #     # Hostmanager aliases for name resolution
  #     if Vagrant.has_plugin?("vagrant-hostmanager")
  #       db.hostmanager.aliases = %w(db)
  #     end

  #     # Primary host-only private network (static IP)
  #     db.vm.network "private_network", ip: "192.168.56.11"

  #     # Alternative DHCP configuration option (referenced in guide):
  #     # db.vm.network "private_network", type: "dhcp"

  #     # VirtualBox provider resource limits
  #     db.vm.provider "virtualbox" do |vb|
  #       vb.name = "demo-db"
  #       vb.cpus = 1
  #       vb.memory = 1024
  #     end

  #     # Shared synced folder across machines
  #     db.vm.synced_folder "./src", "/srv/src", create: true

  #     # Combining provisioners: initial package update
  #     db.vm.provision "shell", inline: <<-SHELL
  #       sudo apt update
  #       sudo apt install -y postgresql
  #     SHELL

  #     # Hostname resolution fallback via /etc/hosts shell provisioning
  #     db.vm.provision "shell", inline: <<-SHELL
  #       cat <<'EOF' | sudo tee -a /etc/hosts
  # #{hosts}
  # EOF
  #     SHELL

  #     # Run-time execution control examples: "always" vs "once"
  #     db.vm.provision "shell", inline: "echo 'Database provisioning finished.'", run: "once"
  #   end

  #   # ==========================================
  #   # Web / Application Machine (web)
  #   # ==========================================
  #   config.vm.define "web" do |web|
  #     web.vm.hostname = "web.local"

  #     # Hostmanager aliases for name resolution
  #     if Vagrant.has_plugin?("vagrant-hostmanager")
  #       web.hostmanager.aliases = %w(web)
  #     end

  #     # Primary host-only private network (static IP)
  #     web.vm.network "private_network", ip: "192.168.56.10"

  #     # Multiple private networks pattern (e.g., dedicated backend or monitoring LAN)
  #     web.vm.network "private_network", ip: "192.168.57.20"

  #     # VirtualBox provider resource limits
  #     web.vm.provider "virtualbox" do |vb|
  #       vb.name = "demo-web"
  #       vb.cpus = 2
  #       vb.memory = 2048
  #     end

  #     # Shared synced folder across machines
  #     web.vm.synced_folder "./src", "/srv/src", create: true

  #     # Inline shell provisioning to install Nginx
  #     web.vm.provision "shell", inline: <<-SHELL
  #       sudo apt update
  #       sudo apt install -y nginx
  #     SHELL

  #     # External script provisioning pattern (e.g., path: "setup.sh")
  #     # web.vm.provision "shell", path: "setup.sh"

  #     # Advanced provisioner integration example (Ansible)
  #     # web.vm.provision "ansible" do |ansible|
  #     #   ansible.playbook = "playbook.yml"
  #     # end

  #     # Hostname resolution fallback via /etc/hosts shell provisioning
  #     web.vm.provision "shell", inline: <<-SHELL
  #       cat <<'EOF' | sudo tee -a /etc/hosts
  # #{hosts}
  # EOF
  #     SHELL

  #     # Run-time execution control example
  #     web.vm.provision "shell", inline: "echo 'Web server startup verified.'", run: "always"
  #   end

  #   # ==========================================
  #   # Triggers
  #   # ==========================================
  #   # Block web machine startup until the database port (5432) is actively listening
  #   config.trigger.after :up do |t|
  #     t.only_on = ["web"]
  #     t.run = {
  #       inline: "until nc -z 192.168.56.11 5432; do echo 'Waiting for DB...'; sleep 2; done"
  #     }
  #   end
  # end


  # Disable automatic box update checking. If you disable this, then
  # boxes will only be checked for updates when the user runs
  # `vagrant box outdated`. This is not recommended.
  # config.vm.box_check_update = false

  # Create a forwarded port mapping which allows access to a specific port
  # within the machine from a port on the host machine. In the example below,
  # accessing "localhost:8080" will access port 80 on the guest machine.
  # NOTE: This will enable public access to the opened port
  # config.vm.network "forwarded_port", guest: 80, host: 8080

  # Create a forwarded port mapping which allows access to a specific port
  # within the machine from a port on the host machine and only allow access
  # via 127.0.0.1 to disable public access
  # config.vm.network "forwarded_port", guest: 80, host: 8080, host_ip: "127.0.0.1"

  # Create a private network, which allows host-only access to the machine
  # using a specific IP.
  # config.vm.network "private_network", ip: "192.168.33.10"

  # Create a public network, which generally matched to bridged network.
  # Bridged networks make the machine appear as another physical device on
  # your network.
  # config.vm.network "public_network"

  # Share an additional folder to the guest VM. The first argument is
  # the path on the host to the actual folder. The second argument is
  # the path on the guest to mount the folder. And the optional third
  # argument is a set of non-required options.
  # config.vm.synced_folder "../data", "/vagrant_data"

  # Disable the default share of the current code directory. Doing this
  # provides improved isolation between the vagrant box and your host
  # by making sure your Vagrantfile isn't accessible to the vagrant box.
  # If you use this you may want to enable additional shared subfolders as
  # shown above.
  # config.vm.synced_folder ".", "/vagrant", disabled: true

  # Provider-specific configuration so you can fine-tune various
  # backing providers for Vagrant. These expose provider-specific options.
  # Example for VirtualBox:
  #
  # config.vm.provider "virtualbox" do |vb|
  #   # Display the VirtualBox GUI when booting the machine
  #   vb.gui = true
  #
  #   # Customize the amount of memory on the VM:
  #   vb.memory = "1024"
  # end
  #
  # View the documentation for the provider you are using for more
  # information on available options.

  # Enable provisioning with a shell script. Additional provisioners such as
  # Ansible, Chef, Docker, Puppet and Salt are also available. Please see the
  # documentation for more information about their specific syntax and use.
  # config.vm.provision "shell", inline: <<-SHELL
  #   apt-get update
  #   apt-get install -y apache2
  # SHELL
end